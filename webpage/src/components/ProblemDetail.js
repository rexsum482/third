import React from "react";

const ProblemDetail = ({
    category,
    selectedConcerns,
    details,
    files,
    onToggle,
    onDetailChange,
    onFileChange,
    onContinue,
  }) => {
    return (
        <></>
    );
};

export default ProblemDetail;